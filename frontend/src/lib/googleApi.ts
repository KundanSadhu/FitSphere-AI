export async function addWorkoutToCalendarAndTasks(planName: string, dayName: string, exercises: any[]): Promise<void> {
  throw new Error('Google Calendar & Tasks export is currently disabled. The required Google OAuth scopes have been removed.');
}
